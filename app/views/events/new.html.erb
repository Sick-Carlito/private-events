

<%= form_for @event do |f| %>

  <%= f.hidden_field :event_id, value: @event.id %>

  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :description %>
  <%= f.text_area :description %>

  <%= f.label :date %>
  <%= f.date_field :date %>

  <%= f.submit "Submit Event" %>



<%end%>